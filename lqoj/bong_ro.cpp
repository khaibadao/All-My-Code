#include<bits/stdc++.h>
using namespace std;
int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	int n; cin >> n;
	long d; cin >> d;
	long a[n + 1];
	map <int>
	for(int i = 1 ; i <= n ; ++i) cin >> a[i];
	
	return 0;
}
