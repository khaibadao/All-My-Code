#include <bits/stdc++.h>
#define en '\n'
#define Siz(s) s.size()
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t; cin >> t;
    while(t--){
    	long long n; cin >> n;
    	long res = sqrt(n);
    	cout << (long)res << en;
	}
    return 0;
}

