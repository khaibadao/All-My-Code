#include<bits/stdc++.h>
using namespace std;

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	int n; cin >> n;
	int k; cin >> k;
	int x[k + 1];
	string xau = "";
	int dem = 0;
	for(int i = 1 ; i <= k ; ++i) cin >> x[i];
	for(int i = 0 ; i <= n ; ++i){
		
	}
	return 0;
}
