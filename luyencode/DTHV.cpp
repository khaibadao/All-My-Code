#include <bits/stdc++.h>
#define Siz(s) s.size()
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    long n; cin >> n;
    cout << fixed << setprecision(1) << ((long double)n/sqrt(2))*((long double)n/sqrt(2));
    return 0;
}

