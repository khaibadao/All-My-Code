#include<bits/stdc++.h>
using namespace std;
#define MOD 1000000007
long long f[1000001];
int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	int n; cin >> n;
	int a[n + 1];
	for(int i = 0 ; i < n ; ++i) cin >> a[i];
	int sum = 0;
	int i = 0, j = 1;
	while(i < n && j < n){
		if(abs(a[i] - a[j]) < )
	}
	return 0;
}
